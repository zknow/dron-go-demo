---
kind: pipeline
name: default

trigger:
  event:
    - push
    
steps:
- name: backend
  image: golang:1.13-alpine
  environment:
    USERNAME:
      from_secret: username
  commands:
  - go build
  - go run .
  - echo $USERNAME
  - echo $USERNAME > a.txt
  - cat a.txt
